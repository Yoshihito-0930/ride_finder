# fly.toml app configuration file generated for ride-finder-ancient-feather-1535 on 2024-05-21T02:11:01+09:00
#
# See https://fly.io/docs/reference/configuration/ for information about how to use this file.
#

app = 'ride-finder-ancient-feather-1535'
primary_region = 'nrt'
console_command = '/rails/bin/rails console'

[build]
  dockerfile = "Dockerfile"

[env]
  RAILS_ENV = "production"
  DATABASE_URL = "postgres://<DB_USER>:<DB_PASSWORD>@<DB_HOST>/<DB_NAME>"

[deploy]
  release_command = "bin/rails db:migrate"

[http_service]
  internal_port = 3000
  force_https = true
  auto_stop_machines = true
  auto_start_machines = true
  min_machines_running = 0
  processes = ['app']

[[vm]]
  memory = '1gb'
  cpu_kind = 'shared'
  cpus = 1
